# finance-site

Run the following to open the project on your VS Code environment:

```bash
cd finance_app
./setup.sh
source venv/bin/activate
reflex run
```

Note: the app requires a compatible Python version (3.12 is recommended). The setup script now selects a supported interpreter automatically.

